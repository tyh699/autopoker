{
  "name": "@poker/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:win": "tauri build --bundles nsis,msi",
    "build:linux": "tauri build --bundles deb,rpm"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
